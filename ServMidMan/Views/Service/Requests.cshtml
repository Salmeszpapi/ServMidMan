@model IEnumerable<ServMidMan.Models.Service>

@{
    ViewData["Title"] = "Requests";
}

<h1>Incoming and Outgoing Requests</h1>

<div>
    <label>
        Show:
        <input type="radio" name="requestType" value="incoming" checked>Incoming
        <input type="radio" name="requestType" value="outgoing">Outgoing
    </label>
</div>

<table class="table">
    <thead>
        <tr>
            <th>Product ID</th>
            <th>User ID</th>
            <th>Approximately Finish Date</th>
            <th>Price</th>
            <th>Approved</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@*         @foreach (var request in Model)
        {
            if ((requestType == "incoming" && request.UserId != loggedInUserId) || (requestType == "outgoing" && request.UserId == loggedInUserId))
            {
                <tr>
                    <td>@request.ProductId</td>
                    <td>@request.UserId</td>
                    <td>@request.ApproximetlyFinishDate.ToShortDateString()</td>
                    <td>@request.Price</td>
                    <td>@(request.Approved ? "Approved" : "Pending")</td>
                    <td>
                        @if (requestType == "incoming")
                        {
                            <button class="btn btn-primary" onclick="acceptRequest(@request.Id)">Accept</button>
                        }
                        else
                        {
                            <button class="btn btn-danger" onclick="deleteRequest(@request.Id)">Delete</button>
                        }
                    </td>
                </tr>
            }
        } *@
    </tbody>
</table>

@section scripts {
    <script>
        function acceptRequest(requestId) {
            // Implement logic to accept the incoming request
            alert("Request Accepted!");
        }

        function deleteRequest(requestId) {
            // Implement logic to delete the outgoing request
            alert("Request Deleted!");
        }
    </script>
}